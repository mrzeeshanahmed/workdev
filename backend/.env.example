# Example environment for backend services
# Rename to .env and fill values for local DB-backed testing

# Example environment for backend services
# Rename to .env and fill values for local DB-backed testing

# Postgres connection string used by runtime DB-backed mode and migration tooling.
# When empty, the app falls back to an in-memory store (useful for fast local dev and tests).
# Example: postgresql://postgres:password@localhost:5432/workdev_test
DATABASE_URL=

# Server port (optional)
# PORT=4000

# Node environment
# NODE_ENV=development

# NOTE: Older templates referenced DB_ENABLED — this variable is not used by the
# runtime. Presence/absence of DATABASE_URL controls DB-backed vs in-memory mode.
