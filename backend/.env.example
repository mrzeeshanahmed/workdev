# Example environment for backend services
# Rename to .env and fill values for local DB-backed testing

# Postgres connection string used by migration tooling and runtime DB-backed mode
# Example: postgresql://postgres:password@localhost:5432/workdev_test
DATABASE_URL=

# If true, services will attempt DB-backed mode. When DATABASE_URL is empty, services
# fall back to in-memory stores (good for fast tests and local dev).
DB_ENABLED=true

# Optional: node environment
NODE_ENV=development
# Example environment variables for backend
DATABASE_URL=postgres://localhost:5432/workdev_test
PORT=4000
NODE_ENV=development
